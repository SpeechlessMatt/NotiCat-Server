// Copyright 2026 Czy_4201b
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

package main

// THIS FILE IS GENERATED BY Gemini 3 & Czy_4201b
//
// Author 1: Gemini 3 Flash (It is really strong)
// Author 2: Czy_4201b <speechlessmatt@qq.com>
// Created: 2026-02-03
//
// "Code is a craft; automation is the soul."
// 让代码自动生长，让灵感专注核心。
// --Gemini

import (
	"encoding/json"
	"flag"
	"fmt"
	"os"
	"path/filepath"
	"text/template"
	"time"
)

// ClientDetail 对应单个 Client 的配置
type ClientDetail struct {
	Client      string           `json:"client"`
	Name        string           `json:"name"`
	URL         string           `json:"url"`
	Description string           `json:"description"`
	Credentials []string         `json:"credentials"`
	Extra       []map[string]any `json:"extra"`
}

// InfoConfig 对应 info.json 的完整包装结构
type InfoConfig struct {
	Name           string         `json:"name"`
	Version        string         `json:"version"`
	BuildTime      string         `json:"build_time"`
	Owner          string         `json:"owner"`
	Description    string         `json:"description"`
	SupportClients []ClientDetail `json:"support_clients"`
}

const (
	clientsSource = "cmd/gen/clients.json"
	infoTarget    = "internal/meta/data/info.json"
	goTarget      = "internal/bridge/clients.go"
	mdSource  = "cmd/gen/CLIENTS.md"
	mdTarget  = "internal/meta/data/CLIENTS.md"
	iconSource = "cmd/gen/icon.png"
	iconTarget = "internal/meta/data/icon.png"
)

const goTemplate = `// Code generated by cmd/gen; DO NOT EDIT.
//go:generate go run ../../cmd/gen/main.go
package bridge

type Client string

// register Clients
const (
{{- range .SupportClients}}
	Client{{.Name}} Client = "{{.Client}}"
{{- end}}
)
// const register

var SupportedClients = map[Client]bool{
{{- range .SupportClients}}
	Client{{.Name}}: true,
{{- end}}
}
// map register
// end register
`

// updateInfoJSON 负责将配置写回 info.json
func updateInfoJSON(targetPath string, config InfoConfig) {
	// 每次生成时自动更新 BuildTime 为当前时间
	config.BuildTime = time.Now().Format(time.RFC3339)

	// 将结构体序列化为带缩进的 JSON
	newJSON, err := json.MarshalIndent(config, "", "    ")
	if err != nil {
		fmt.Printf("❌ 序列化 info.json 失败: %v\n", err)
		return
	}

	err = os.WriteFile(targetPath, newJSON, 0o644)
	if err != nil {
		fmt.Printf("❌ 写入 info.json 失败: %v\n", err)
		return
	}
	fmt.Println("✅ 已同步至 internal/meta/data/info.json")
}

func copyFile(src, dst string) {
    data, err := os.ReadFile(src)
    if err != nil {
        fmt.Printf("⚠️  跳过: 找不到源文件 %s\n", src)
        return
    }

    err = os.WriteFile(dst, data, 0644)
    if err != nil {
        fmt.Printf("❌ 复制失败: %v\n", err)
        return
    }
    fmt.Printf("✅ %s 已同步至 internal/meta/data/\n", src)
}

func main() {
	var rootDir string
	flag.StringVar(&rootDir, "root", ".", "Project root directory")
	flag.Parse()

	originJSON := filepath.Join(rootDir, clientsSource)
	originReadme := filepath.Join(rootDir, mdSource)
	originIcon := filepath.Join(rootDir, iconSource)

	goFile := filepath.Join(rootDir, goTarget)
	infoFile := filepath.Join(rootDir, infoTarget)
	mdFile := filepath.Join(rootDir, mdTarget)
	iconFile := filepath.Join(rootDir, iconTarget)

	// 1. 读取母本 JSON
	data, err := os.ReadFile(originJSON)
	if err != nil {
		fmt.Printf("❌ 错误: 找不到母本文件 %s\n", originJSON)
		return
	}

	// 2. 解析 JSON 到结构体
	// 注意：你的母本 clients.json 建议直接写成满足 InfoConfig 结构的 JSON
	var config InfoConfig
	if err := json.Unmarshal(data, &config); err != nil {
		fmt.Printf("❌ JSON 解析失败: %v\n", err)
		return
	}

	// 3. 生成 Go 文件
	tmpl, err := template.New("gen").Parse(goTemplate)
	if err != nil {
		fmt.Printf("❌ 模板解析失败: %v\n", err)
		return
	}

	f, err := os.Create(goFile)
	if err != nil {
		fmt.Printf("❌ 无法创建 Go 文件: %v\n", err)
		return
	}
	defer f.Close()

	if err := tmpl.Execute(f, config); err != nil {
		fmt.Printf("❌ 渲染模板失败: %v\n", err)
		return
	}

	// 4. 同步更新 info.json
	updateInfoJSON(infoFile, config)
	copyFile(originReadme, mdFile)
	copyFile(originIcon, iconFile)

	fmt.Printf("\n✨ 恭喜！已根据 %d 个客户端信息完成全链路同步。\n", len(config.SupportClients))
	fmt.Printf("📍 代码位置: %s\n", goFile)
	fmt.Printf("📍 配置位置: %s\n", infoFile)
	fmt.Printf("📍 配置位置: %s\n", mdFile)
	fmt.Printf("📍 配置位置: %s\n", iconFile)
}
